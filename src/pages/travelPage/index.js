import { Box } from "@mui/material";
import React from "react";
import { Layout } from "../../components/Layout";

/**
 * @author
 * @function TravelPage
 **/

export const TravelPage = (props) => {
  return (
    <Layout>
      <Box mt={15}>Travel</Box>
    </Layout>
  );
};
