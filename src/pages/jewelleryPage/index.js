import { Box } from "@mui/material";
import React from "react";
import { Layout } from "../../components/Layout";

/**
 * @author
 * @function JewelsPage
 **/

export const JewelsPage = (props) => {
  return (
    <Layout>
      <Box mt={15}>Jewels</Box>
    </Layout>
  );
};
